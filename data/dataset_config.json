{
  "_DEPRECATED": "Ce fichier a ete fusionne dans data/config.json (blocs frame_detection + hsv_ranges + mask_parameters + gt_classes + series). Les anciens chargeurs (pipeline/create_masks.py) continuent de fonctionner par fallback.",
  "_migration": "Pour acceder a la config HSV : cfg = json.load(open('data/config.json')); cfg['hsv_ranges']"
}
