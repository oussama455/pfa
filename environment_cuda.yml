# DEPRECATED -- ce fichier a ete fusionne dans environment.yml.
#
# Pour installer avec support CUDA, suis les instructions en tete
# de environment.yml :
#
#     conda env create -f environment.yml
#     conda activate pfa
#     pip uninstall -y torch torchvision
#     pip install torch==2.2.2 torchvision==0.17.2 \
#         --index-url https://download.pytorch.org/whl/cu121
#
# Ce fichier sera supprime lors du prochain nettoyage du repo
# (`git rm environment_cuda.yml`).
